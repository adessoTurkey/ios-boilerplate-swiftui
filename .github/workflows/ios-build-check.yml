name: iOS Build Check Workflow

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:
    name: Build scheme 
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          pod install --repo-update
        shell: bash
      - name: Build
        run: |
          xcodebuild -workspace boilerplate_ios_swiftui.xcworkspace -scheme boilerplate-ios-swiftui clean build -sdk iphoneos -configuration Development CODE_SIGNING_ALLOWED=No          
